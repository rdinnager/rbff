# Default make file
PKG_CPPFLAGS=-isystem/usr/include/suitesparse -iquote../src/linear-algebra -iquote../src/project -iquote../src/mesh -iquote../src/rectangle-bin-pack
PKG_LIBS=$(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) -lcholmod

# Include all C++ files in src/ and its subdirectories: 
SOURCES=main.cpp RcppExports.cpp @SUBDIR_SOURCES@

# Obtain the object files
OBJECTS=$(SOURCES:.cpp=.o) 

# Make the shared object
all: $(SHLIB)

# Provide recipe to remove all objects
clean:
	@rm -f $(OBJECTS)
